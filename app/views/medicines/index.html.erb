<p id="notice"><%= notice %></p>

<h1>Medication</h1>
<p>List of appropriate medication for related symptoms/ diseases. The 1-1-1 format signifies that you need to take the medication once in the morning, afternoon and evening.</p>

<table class="table table-striped table-bordered">
  <tr>
    <th class="center">Disease / Symptom</th>
    <th class="center">Medication</th>
    <th class="center">Additional notes</th>
    <th class="center">Action</th>
  </tr>
  <thead>
  </thead>

  <tbody>
    <% @medicines.each do |medicine| %>
      <tr>
        <td><%= medicine.disease %></td>
        <td><%= medicine.med %></td>
        <td><%= medicine.note %></td>
        <td>
        <%= link_to "Update",edit_medicine_path(medicine), class: "btn btn-link" %>
        <%= link_to "Delete",medicine, method: :delete, data: {confirm: "Are you sure?"}, class: "btn btn-link" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Add medication', new_medicine_path, class: "btn btn-link" %>
<div class="center">
   <%= will_paginate @medicines, renderer: BootstrapPagination::Rails %>
</div>