
<p id="notice"><%= notice %></p>

<h1>Missing Persons</h1>
<p>Please update for any missing persons with an image, detailed description and last known location. If the person has been located, please update the description box as 'FOUND' </p>

<table class="table table-striped table-bordered">
  <tr>
    <th class="center">Name</th>
    <th class="center">Description</th>
    <th class="center">Last known location</th>
    <th class="center">Image</th>
    <th class="center">Action</th>
  </tr>
  <thead>
  </thead>

  <tbody>
    <% @people.each do |person| %>
      <tr>
        <td><%= person.name %></td>
        <td><%= person.description %></td>
        <td><%= person.location %></td>
        <td><%= link_to image_tag(person.image.url(:thumb)), person %></td>
        <td>
        <li><%= link_to "Update",edit_person_path(person), class: "btn btn-link" %></li>
        <li><%= link_to "Delete",person, method: :delete, data: {confirm: "Are you sure?"}, class: "btn btn-link" %></li>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Add Person', new_person_path, class: "btn btn-link" %>
