<p id="notice"><%= notice %></p>

<h1>Functioning Fuel Depos</h1>
<p>Please list all functioning fuel depos and if they have an adequate supply of petrol / diesel.</p>

<table class="table table-striped table-bordered">
  <tr>
    <th class="center">Location</th>
    <th class="center">Date</th>
    <th class="center">Time</th>
    <th class="center">Image</th>
    <th class="center">Supply</th>
    <th class="center">Action</th>
  </tr>
  <thead>
  </thead>

  <tbody>
    <% @fuels.each do |fuel| %>
      <tr>
        <td><%= fuel.location %></td>
        <td><%= fuel.date %></td>
        <td><%= fuel.time %></td>
        <td><%= link_to image_tag(fuel.image.url(:thumb)), fuel %></td>
        <td><%= fuel.supply %>
        <td>
        <li><%= link_to "Update",edit_fuel_path(fuel), class: "btn btn-link" %></li>
        <li><%= link_to "Delete",fuel, method: :delete, data: {confirm: "Are you sure?"}, class: "btn btn-link" %></li>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>
<%= link_to 'Add Fuel bunk', new_fuel_path, class: "btn btn-link" %>

